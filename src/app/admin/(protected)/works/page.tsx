import Link from 'next/link'; import { requireAdmin } from '@/lib/session'; import { listWorks } from '@/services/cms';
export const dynamic='force-dynamic'; export default async function WorksAdmin(){await requireAdmin();const works=await listWorks();return <div><div className="flex items-center justify-between gap-4"><div><p className="text-sm text-slate-500">콘텐츠 관리</p><h1 className="mt-2 text-3xl font-bold">작업사례</h1></div><Link href="/admin/works/new" className="inline-flex min-h-11 items-center rounded-xl bg-red-700 px-4 text-sm font-bold text-white">사례 등록</Link></div><ul className="mt-6 grid gap-3">{works.length?works.map(w=><li key={w.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-slate-200 bg-white p-5"><div><p className="font-bold">{w.title}</p><p className="mt-1 text-sm text-slate-500">/{w.slug} · 사진 {w.imageCount}장 · {w.published?'공개':'비공개'}</p></div><Link href={`/admin/works/${w.id}`} className="min-h-11 rounded-lg border border-slate-300 px-4 py-3 text-sm font-bold">수정</Link></li>):<li className="rounded-2xl bg-slate-50 p-6 text-sm text-slate-500">등록된 작업사례가 없습니다.</li>}</ul></div>;}
