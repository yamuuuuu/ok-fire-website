import { requireAdmin } from '@/lib/session';import { listSettings } from '@/services/cms';import { SettingsEditor } from '@/components/admin/settings-editor';export const dynamic='force-dynamic';export default async function Settings(){const admin=await requireAdmin('SUPER_ADMIN');return <div><p className="text-sm text-slate-500">최고 관리자</p><h1 className="mt-2 text-3xl font-bold">사이트 설정</h1><div className="mt-6 max-w-3xl"><SettingsEditor items={await listSettings()} adminId={admin.id}/></div></div>;}
