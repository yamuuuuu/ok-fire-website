import { ApiError,failure,readJson,requireSameOrigin,success } from '@/lib/http'; import { requireAdmin } from '@/lib/session'; import { resetPassword } from '@/services/cms'; import { passwordResetSchema } from '@/validations/cms';
export async function POST(r:Request,c:{params:Promise<{id:string}>}){try{requireSameOrigin(r);const a=await requireAdmin('SUPER_ADMIN');const p=passwordResetSchema.safeParse(await readJson(r));if(!p.success)throw new ApiError(422,'VALIDATION_ERROR','비밀번호는 16~128자로 입력해주세요.');return success(await resetPassword((await c.params).id,a.id,p.data.password));}catch(e){return failure(e);}}
